{
  "name": "first-electron-app",
  "version": "1.0.0",
  "description": "first electron app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vanbo",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.1.0"
  }
}
